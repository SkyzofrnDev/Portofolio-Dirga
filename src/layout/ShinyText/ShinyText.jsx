import React from 'react'
import { ShinyTextComponent } from '../../components'

const ShinyText = () => {
  return (
    <div>
  
    <ShinyTextComponent text="Just some shiny text!" disabled={false} speed={3} className='custom-class' /></div>
  )
}

export default ShinyText